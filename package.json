{
  "name": "meari",
  "version": "1.0.0",
  "description": "nexon game maintenance checker",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-packager ./ --platform=win32 --arch=x64 --overwrite --electron-version=21.2.3 --icon=./src/public/assets/icon.png",
    "build:win64": "electron-builder -c.electronVersion=21.2.3 --win portable --x64"
  },
  "author": "gogotr",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.1.3",
    "cheerio": "^1.0.0-rc.12"
  },
  "devDependencies": {
    "electron-builder": "^23.6.0",
    "electron-packager": "^17.1.1"
  },
  "build": {
    "productName": "MEARI",
    "icon": "./src/public/assets/icon2.ico"
  }
}
